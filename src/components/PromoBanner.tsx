const PromoBanner = () => {
  return (
    <div 
      className="bg-primary text-primary-foreground py-3 text-center font-semibold text-sm uppercase tracking-wider"
      style={{
        position: 'relative',
        zIndex: 1,
      }}
    >
      FREE SHIPPING ON ALL ORDERS!
    </div>
  );
};

export default PromoBanner;
